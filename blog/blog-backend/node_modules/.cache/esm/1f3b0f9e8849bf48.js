let mongoose;_884‍.x([["default",()=>_884‍.o]]);_884‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const PostSchema = new Schema({
    title: String,
    body: String,
    tags: [String],
    publisgedDate: {
        type: Date,
        default: Date.now,
    },
    user: {
        _id: mongoose.Types.ObjectId,
        username: String
    },
});

const Post = mongoose.model('Post', PostSchema);
_884‍.d(Post);