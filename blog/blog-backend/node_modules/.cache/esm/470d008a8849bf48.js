let api,jwtMiddleware;_884‍.w("./api",[["default",["api"],function(v){api=v}]]);_884‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();
const Koa = require('koa');
const Router = require('koa-router');
const bodyParser = require('koa-bodyparser');
const mongoose = require('mongoose');




const { PORT, MONGO_URI } = process.env;

mongoose
.connect(MONGO_URI)
    .then(() => {
        console.log('Connected to MongoDB');
    })
    .catch(e => {
        _884‍.g.console.log(e);
    })


const app = new Koa();
const router = new Router();

router.use('/api', api.routes()); /** api 라우트 적용 */

app.use(bodyParser());
app.use(jwtMiddleware);

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
    _884‍.g.console.log('Listening to port %d', port);
})
